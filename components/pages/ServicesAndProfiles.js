import React from 'react';
import { View } from 'react-native';
import Header from '../../components/toServicesAndProfiles/header/index';

export default () => {
    return (
        <View style={{ backgroundColor: 'cyan', flex: 1 }}>
            <Header />
        </View>
    );
}